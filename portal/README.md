# SenNet Portal

Frontend del Portal SenNet basado en Next.js (App Router), TailwindCSS y componentes shadcn/ui.

## Requisitos
- Node.js LTS (recomendado Node 20)
- npm

## Comandos

```bash
cd portal
npm install
npm run dev
npm run build && npm run start
```
